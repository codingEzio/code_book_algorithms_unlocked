# Status
#   core
#       solved by myself        : not yet
#       submitted to leetcode   : not yet
#   experiment
#       self-contained (stdlib) : \
#       runnable locally        : \


class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next


def add_two_numbers(l1: ListNode, l2: ListNode) -> ListNode:
    """
    Given two non-empty linked list, e.g. [1, 2, 3] (321), [1, 9, 2] (291),
    since the digits are stored in reverse order, the output should be 612,
    i.e. [2, 1, 6].

    There won't be any case there is a leading zero except the "0" case :)
    """
    pass


def main():
    # 321 200
    a, b = ListNode(), ListNode()
    pass


if "__main__" == __name__:
    main()
